{
  "scripts": {
    "dev": "run-p serve watch",
    "serve": "live-server public --host=localhost --port=3000 --no-browser",
    "compile": "tailwindcss -i public/css/input.css -o public/css/output.css",
    "watch": "npm run compile -- --watch"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.2.2"
  }
}
